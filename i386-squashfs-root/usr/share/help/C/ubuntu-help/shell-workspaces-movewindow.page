<page xmlns="http://projectmallard.org/1.0/"
      type="topic"
      style="task"
      id="shell-workspaces-movewindow">

  <info>

    <link type="guide" xref="shell-windows#working-with-workspaces"/>
    <link type="seealso" xref="shell-workspaces"/>
    
    <desc>Open the workspace switcher and drag the window to a different workspace.</desc>

    <revision version="11.04" date="2011-04-29" status="final"/>
    <credit type="author">
      <name>GNOME Documentation Project</name>
      <email>gnome-doc-list@gnome.org</email>
    </credit>

    <include href="legal.xml" xmlns="http://www.w3.org/2001/XInclude"/>
    
  </info>

<title>Move windows to a different workspace</title>

<p>You can move a window between workspaces:</p>

<steps>
 <item>
  <p>Open the <link xref="unity-launcher-intro">Launcher</link> and click the
<gui>workspace switcher</gui> button near the bottom.</p>
 </item>
 <item>
  <p>Drag the window to the workspace you choose.</p>
 </item>
</steps>

<p>A faster way of moving windows between workspaces is by using the keyboard:</p>
  <list>
   <item>
    <p>Press <keyseq><key>Ctrl</key><key>Alt</key><key>Shift</key><key>	&#8594;</key></keyseq>
to move the window to the workspace to the right of the current workspace.</p>
   </item>
   <item>
    <p>Press <keyseq><key>Ctrl</key><key>Alt</key><key>Shift</key><key>	&#8592;</key></keyseq>
to move the window to the workspace to the left of the current workspace.</p>
   </item>
   <item>
    <p>Press <keyseq><key>Ctrl</key><key>Alt</key><key>Shift</key><key>	&#8595;</key></keyseq>
to move the window to the workspace below the current workspace.</p>
   </item>
   <item>
    <p>Press <keyseq><key>Ctrl</key><key>Alt</key><key>Shift</key><key>&#8593;</key></keyseq>
to move the window to the workspace above the current workspace.</p>
   </item>
  </list>

</page>
