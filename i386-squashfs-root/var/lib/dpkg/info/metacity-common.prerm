#!/bin/sh
set -e
# Automatically added by dh_installcatalogs
if [ "$1" = "remove" ] || [ "$1" = "upgrade" ]; then
	update-catalog --quiet --remove --super /etc/sgml/metacity-common.cat
fi
# End automatically added section
