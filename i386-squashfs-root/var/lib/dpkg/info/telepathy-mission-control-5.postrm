#!/bin/sh
set -e
# Automatically added by dh_apparmor
if [ "$1" = "purge" ]; then
    rm -f "/etc/apparmor.d/disable/usr.lib.telepathy" || true
    rm -f "/etc/apparmor.d/force-complain/usr.lib.telepathy" || true
    rm -f "/etc/apparmor.d/local/usr.lib.telepathy" || true
    rmdir /etc/apparmor.d/local 2>/dev/null || true
fi
# End automatically added section
