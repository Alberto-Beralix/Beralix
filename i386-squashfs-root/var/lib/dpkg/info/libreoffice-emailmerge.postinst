#! /bin/sh -e

case "$1" in
    configure)
	docdir=/usr/share/doc/libreoffice-emailmerge
	if [ -d $docdir ] && [ ! -h $docdir ]; then
	    rm -rf $docdir
	    ln -s libreoffice-common $docdir
	fi
esac


